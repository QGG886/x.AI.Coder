# WinForms → WPF 代码转换代理

## 任务概述

将 XIR 项目的 WinForms 源代码转换为 WPF 框架代码。

## 核心原则

- 完整性优先：严禁省略任何代码，必须完整读取并转换所有内容
- 保持命名：所有变量名、方法名、事件名必须保持不变
- 保持逻辑：代码逻辑、顺序必须完全一致
- 简洁日志：每步骤只记录当前步骤的关键信息
- 支持文件夹结构：输出会保持与输入相同的目录结构

## 使用模式

**交互式模式**：
- 如果未提供任务参数（{FILES}、{OUTPUT_DIR}、{LOG_FILE} 为空），则进入交互式模式
- 适合单文件调试和快速测试

**批量模式**：
- 如果已提供任务参数，则直接使用这些参数执行任务
- 适合批量处理多个文件

## 重要说明

- **文件路径**：{FILES} 是绝对路径，需要使用绝对路径读取文件
- **输出目录**：{OUTPUT_DIR} 是绝对路径
- **日志文件**：{LOG_FILE} 是文件名
- **步骤流程**：共 5 个步骤，第 4 步会检查所有步骤的日志并进行回归操作
- **规则加载**：所有步骤的规则加载都遵循 `prompt/rules/rule_index.md` 的指引

## 任务参数

- **文件列表**：{FILES}（绝对路径，多个文件用逗号分隔）
- **输出目录**：{OUTPUT_DIR}（绝对路径）
- **日志文件**：{LOG_FILE}（文件名）

---

## 开始执行

### 交互式参数输入（仅在参数为空时执行）

**检查参数状态**：
- 如果 {FILES}、{OUTPUT_DIR}、{LOG_FILE} 都已提供，跳过此步骤，直接进入"第一步"
- 如果任何一个参数为空，则执行以下交互式输入流程

**交互式输入流程**：

1. **输入文件列表**：
   - 请提供需要转换的 WinForms 文件列表（绝对路径，多个文件用逗号分隔）
   - 文件类型包括：
     - WinForms 主代码文件（.cs）
     - WinForms 设计器文件（.Designer.cs）
   - 示例：`C:\Project\winform\Form1.cs,C:\Project\winform\Form1.Designer.cs`

2. **输入输出目录**：
   - 请提供输出目录的绝对路径
   - 示例：`C:\Project\wpf`

3. **输入日志文件名**：
   - 请提供日志文件的名称（不需要包含路径）
   - 示例：`Form1.trans.log.md`

**参数验证**：
- 确保所有文件路径都存在
- 确保输出目录可写
- 确保文件列表包含有效的 WinForms 文件

---

### 正式执行步骤

请按照以下步骤依次执行，完成所有文件的转换：

**第一步**：[步骤 1：文件分析](./step_01_file_analysis.md)

完成此步骤后，按照步骤文件末尾的指示继续执行下一步。